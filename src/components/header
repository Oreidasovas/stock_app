import React from "react";

export default function Header() {
  return (
    <header style={{
      backgroundColor: "#222",
      color: "#fff",
      padding: "1rem",
      textAlign: "center"
    }}>
      Controle de Estoque - O Rei das Ovas
    </header>
  );
}
